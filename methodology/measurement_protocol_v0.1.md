### এখানে:
```
Warm-up executions per TX-node pair: 2
Measured executions per TX-node pair: 5
Ground-truth aggregation: Median
```

### অর্থাৎ:

```
TX001 + Low
Warmup × 2
Measure × 5

TX001 + Medium
Warmup × 2
Measure × 5

TX001 + High
Warmup × 2
Measure × 5
```

### ১০টা transaction হলে:

```
10 × 3 × 5
= 150 measured executions
```

### আর warmup:
```
10 × 3 × 2
= 60 warmups
```

### Total:
```
210 executions
```